import { InitializationOptions, Transaction, Account, Event, Unsubscribe, Destroy } from './interfaces';
declare class Blocknative {
    private _storageKey;
    private _connectionId;
    private _dappId;
    private _system;
    private _networkId;
    private _transactionHandlers;
    private _socket;
    private _connected;
    private _sendMessage;
    private _watchedTransactions;
    private _watchedAccounts;
    private _pingTimeout?;
    private _heartbeat?;
    private _destroyed;
    private _onerror;
    transaction: Transaction;
    account: Account;
    event: Event;
    unsubscribe: Unsubscribe;
    destroy: Destroy;
    constructor(options: InitializationOptions);
}
export default Blocknative;
